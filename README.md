# IPS S2026 Copper and Steel Conveyor-Belt RGB-D System

This repository contains the RGB-D data collection and live inference system for:

- capturing copper and steel objects on a moving conveyor belt
- generating training datasets for model development
- running the live segmentation demo for the current conveyor-belt stream
- using the live model weights stored in `rgbd/live/models/`

The detailed user guide is split across the `docs/` folder. Start with `docs/README.md` for the full walkthrough.

## Quick Start

1. Install the Python requirements.
2. Install the Orbbec SDK Python wrapper.
3. From the `rgbd/` folder, run:

   ```bash
   make run
   ```

   to open the data-collection UI, or:

   ```bash
   make demo
   ```

   to launch the live inference demo.

## What Lives Where

- `rgbd/main.py` - data collection UI
- `rgbd/live/live_inference.py` - live demo UI
- `rgbd/processing/` - masking, segmentation, and debug helpers
- `rgbd/camera/` - Orbbec camera interface code
- `rgbd/data/` - saved data generated by the collection UI
- `rgbd/logs/` - runtime logs for the live demo
- `docs/` - separate guides for installation, UI controls, data layout, and models

## Prepared Training Datasets

The real datasets collected by this system and used for model training are already prepared on the CPS server:

```text
/mnt/cps_persistent1_shared/ips_s2026/data-finals/dataset-singles-seg
/mnt/cps_persistent1_shared/ips_s2026/data-finals/dataset-multiples-seg
/mnt/cps_persistent1_shared/ips_s2026/data-finals/dataset-singles-cls
```

Each dataset is already split and ready for training, with roughly 2,000 items per dataset.

## Data Collection Output

When a frame is saved, the collection UI writes the package into `rgbd/data/` using this layout:

```text
rgbd/data/
  images/          # cropped RGB frames
  uncropped_rgb/   # raw full-frame RGB images
  depth/           # depth visualizations saved alongside each frame
  masks/           # binary masks
  labels/          # YOLO polygon labels
  info/            # per-sample metadata text files
  pointcloud/      # saved .ply point clouds
  debug/
    live_capture_debug.log
    live_object_detection_log_YYYYMMDD_HHMMSS.log
    mask_debug/
      *_raw_mask.png
      *_raw_overlay.png
      *_post_close_mask.png
      *_post_close_overlay.png
      *_final_mask.png
      *_debug_sheet.png
```

The save logic is atomic: if any required artifact is missing, the snapshot is not committed.

## Model Files

The live demo ships with model switching built in. The current model names are documented in `docs/models.md` and load from `rgbd/live/models/`.

Current primary weights:

- `best_multiple.pt`
- `best_single.pt`

## Full Guide

Read the docs folder for the complete walkthrough:

- `docs/install.md`
- `docs/live-demo.md`
- `docs/data-collection.md`
- `docs/ui-controls.md`
- `docs/dataset-layout.md`
- `docs/models.md`
